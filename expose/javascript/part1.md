# Part 1
1. Line 9 prints: "Values added: 20"
2. Line 13 prints: "Values added: 20"
3. You should not use `var` because its scope can be confusing. Code several lines below in a different scope can interfere with code in an entirely separate scope.
4. Line 9 prints: "Values added: 20"
5. Line 13 returns an error because `result` doesn't exist in line 13's scope.
6. Line 9 returns an error because line 7 tried to reassign to a `const` variable.
7. Line 13 returns an error because `result` doesn't exist in line 13's scope.
